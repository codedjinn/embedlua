
keyboard = require("keyboard");

Player = {};

function Player:init()

end

function Player:update(time)

end

function Player:handle_keys()

    if (keyboard_mod.is_key_pressed(KEYS.W))

        -- move forward
        -- modify C object
    end

end
